# Copie para .env (ou configure como variáveis de ambiente no EasyPanel).
# NÃO versionar o .env real.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NOME=roh_e_sih
DB_USUARIO=root
DB_SENHA=

APP_AMBIENTE=desenvolvimento   # desenvolvimento | producao
